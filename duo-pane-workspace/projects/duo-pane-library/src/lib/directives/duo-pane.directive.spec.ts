import { DuoPaneDirective } from './duo-pane.directive';

describe('DuoPaneDirective', () => {
  it('should create an instance', () => {
    const directive = new DuoPaneDirective();
    expect(directive).toBeTruthy();
  });
});
